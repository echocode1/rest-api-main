import { Layout } from "@/ui/modules";
import { AmericanRegionData } from "./ui/components";

export const AmericanRegion = () => {
  return (
    <Layout>
      <AmericanRegionData />
    </Layout>
  );
};
