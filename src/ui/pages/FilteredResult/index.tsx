// In FilteredResult
"use client";
import { Layout } from "@/ui/modules";
import { SearchedFilteredResult } from "./ui/component";

export const FilteredResult = () => {
  return (
    <Layout>
      <SearchedFilteredResult />
    </Layout>
  );
};
