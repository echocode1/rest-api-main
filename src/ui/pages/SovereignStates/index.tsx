import { Layout } from "@/ui/modules";
import { AllSovereignState } from "./ui/components";

export const SovereignState = () => {
  return (
    <Layout>
      <AllSovereignState />
    </Layout>
  );
};
